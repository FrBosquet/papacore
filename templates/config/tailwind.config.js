/** @type {import('tailwindcss').Config} */
module.exports = {
  // Tailwind v4 uses CSS-based configuration via @theme
  // This file is optional but needed for IDE autocomplete
  content: ['./src/**/*.{ts,tsx,js,jsx}'],
  important: true, // Makes all utilities use !important
}
